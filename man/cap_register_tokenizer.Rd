% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strategy-tokenizer.R
\name{cap_register_tokenizer}
\alias{cap_register_tokenizer}
\alias{cap_unregister_tokenizer}
\alias{cap_list_tokenizers}
\title{Register, unregister, and list budget tokenizers}
\usage{
cap_register_tokenizer(tokenizer)

cap_unregister_tokenizer(id = NULL)

cap_list_tokenizers()
}
\arguments{
\item{tokenizer}{A \code{capr_tokenizer} from \code{\link[=cap_tokenizer]{cap_tokenizer()}}.}

\item{id}{Tokenizer id to remove; \code{NULL} clears the registry.}
}
\value{
\code{cap_register_tokenizer()} returns the tokenizer invisibly;
\code{cap_unregister_tokenizer()} returns the removed count invisibly;
\code{cap_list_tokenizers()} returns a deterministic data frame.
}
\description{
The tokenizer registry follows the adapter registry contract:
re-registering an identical tokenizer is idempotent, registering a
different tokenizer under an existing id raises
\code{capr_registry_conflict}, and built-in ids cannot be shadowed.
}
