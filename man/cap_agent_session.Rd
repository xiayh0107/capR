% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agent-session.R
\name{cap_agent_session}
\alias{cap_agent_session}
\title{Open an agent session over one source object}
\usage{
cap_agent_session(
  x,
  question = NULL,
  budget = 800L,
  policy = cap_policy(),
  adapter = NULL,
  registry = cap_registry(),
  max_turns = 5L,
  artifact_dir = NULL,
  session = NULL,
  ...
)
}
\arguments{
\item{x}{Source object.}

\item{question}{Optional question used for deterministic intent adjustment.}

\item{budget}{Initial digest budget.}

\item{policy}{Host policy. Its \code{max_followup_budget} seeds the follow-up
budget carried across turns.}

\item{adapter}{Optional explicit adapter or adapter ID.}

\item{registry}{Adapter registry.}

\item{max_turns}{Default turn limit used by \code{\link[=cap_agent_run]{cap_agent_run()}}.}

\item{artifact_dir}{Optional directory; when set, every turn publishes its
canonical artifacts under \verb{turn-NNN/} and the session transcript is
rewritten atomically after every turn.}

\item{session}{Optional host session metadata forwarded to \code{\link[=cap_digest]{cap_digest()}}.}

\item{...}{Context such as \code{label}, \code{uri}, or fixture metadata. Captured
once and replayed into the initial \code{\link[=cap_digest]{cap_digest()}} call and every
follow-up \code{\link[=cap_patch]{cap_patch()}} call.}
}
\value{
A mutable \code{capr_agent_session} environment.
}
\description{
An agent session composes the deterministic core round trip --
\code{cap_digest()}, \code{cap_validate_response()}, \code{cap_gate()}, \code{cap_patch()},
\code{cap_apply_patch()} -- into a stateful multi-turn loop that an external
model can drive. The session never calls a model provider: the model
response is supplied by the host on every turn, and the gate (not the
model) authorizes each follow-up disclosure.
}
\details{
Sessions are process-local: they hold the live source object and the
resolved adapter, so a session cannot be reconstructed from artifacts
alone. Transcripts contain no timestamps and no random identifiers; two
identical runs produce byte-identical canonical transcripts.
}
