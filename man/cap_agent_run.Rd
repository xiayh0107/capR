% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agent-session.R
\name{cap_agent_run}
\alias{cap_agent_run}
\title{Run an agent session loop with a host-supplied model call}
\usage{
cap_agent_run(session, ask, max_turns = NULL, ...)
}
\arguments{
\item{session}{An active \code{capr_agent_session}.}

\item{ask}{\verb{function(text) -> response}; receives the turn prompt and
returns a contract response (R list, JSON string, or JSON file path).}

\item{max_turns}{Optional override of the session turn limit.}

\item{...}{Reserved.}
}
\value{
The session, invisibly. \code{session$stop_reason} is one of
\code{completed}, \code{denied}, \code{budget_exhausted}, \code{stale_source},
\code{invalid_response}, or \code{max_turns}.
}
\description{
Loops prompt -> \code{ask} -> \code{\link[=cap_agent_step]{cap_agent_step()}} until the model answers
without requests, a terminal denial occurs, the source goes stale, or the
turn limit is reached. capR never contacts a model itself: \code{ask} is the
single injection point for the host's model client.
}
