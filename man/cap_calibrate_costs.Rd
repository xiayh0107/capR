% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{cap_calibrate_costs}
\alias{cap_calibrate_costs}
\title{Measure actual field costs against catalog estimates}
\usage{
cap_calibrate_costs(
  x,
  adapter = NULL,
  registry = cap_registry(),
  policy = cap_policy(),
  tokenizer = NULL,
  ...
)
}
\arguments{
\item{x}{Source object.}

\item{adapter}{Optional explicit adapter or adapter ID.}

\item{registry}{Adapter registry.}

\item{policy}{Host policy; execution-denied fields are reported as
unauthorized with \code{NA} measured cost instead of being run.}

\item{tokenizer}{Optional tokenizer id or \code{\link[=cap_tokenizer]{cap_tokenizer()}} used for
measurement.}

\item{...}{Runtime context such as \code{label} or fixture metadata.}
}
\value{
A data frame with one row per field/level: \code{field_id}, \code{level},
\code{timing}, \code{exec}, \code{estimated_cost}, \code{measured_cost}, \code{tokenizer_id},
and \code{authorized}.
}
\description{
Materializes every authorized field/level of an object's catalog once and
reports the measured cost next to the adapter's \code{estimatedCost}. This is
an adapter-authoring aid: \code{estimatedCost} literals are part of an
adapter's identity (they participate in binding signatures and fixture
surfaces), so this helper never rewrites a catalog -- it produces the
evidence an author needs to recost one deliberately.
}
