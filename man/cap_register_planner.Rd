% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strategy-planner.R
\name{cap_register_planner}
\alias{cap_register_planner}
\alias{cap_unregister_planner}
\alias{cap_list_planners}
\title{Register, unregister, and list selection planners}
\usage{
cap_register_planner(planner)

cap_unregister_planner(id = NULL)

cap_list_planners()
}
\arguments{
\item{planner}{A \code{capr_planner} from \code{\link[=cap_planner]{cap_planner()}}.}

\item{id}{Planner id to remove; \code{NULL} clears the registry.}
}
\value{
\code{cap_register_planner()} returns the planner invisibly;
\code{cap_unregister_planner()} returns the removed count invisibly;
\code{cap_list_planners()} returns a deterministic data frame.
}
\description{
The planner registry follows the adapter registry contract:
re-registering an identical planner is idempotent, registering a
different planner under an existing id raises \code{capr_registry_conflict},
and built-in ids cannot be shadowed.
}
