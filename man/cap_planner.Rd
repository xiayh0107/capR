% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strategy-planner.R
\name{cap_planner}
\alias{cap_planner}
\title{Construct a pluggable selection planner}
\usage{
cap_planner(id, version, rank, provider = "host")
}
\arguments{
\item{id}{Planner id (lowercase letters, digits, \code{.}, \verb{_}, \code{-}). The
built-in ids are reserved.}

\item{version}{Semantic version of the planner.}

\item{rank}{\verb{function(candidates, question, policy)} receiving a
read-only list of eligible candidate views (each with \code{field_id},
\code{level}, \code{estimated_cost}, \code{prior_value}, \code{intent_adjustment}, \code{score},
\code{ratio}, \code{timing}, \code{trust}, \code{exec}, \code{label}, \code{intent_tags}) and
returning an integer permutation of \code{seq_along(candidates)}, most
preferred first.}

\item{provider}{Provider label recorded in listings and sidecars.}
}
\value{
A \code{capr_planner} object.
}
\description{
A planner controls exactly one decision: the ranking of already-eligible
field candidates. The runtime keeps candidate construction, eligibility
filtering (interactive timing, execution authorization), the budget
commit walk, and per-field dedup, so a custom planner cannot select an
interactive, execution-denied, or over-budget field. Custom planner ids
are stamped into \code{plan$planner} and the resolution sidecar for audit.
}
