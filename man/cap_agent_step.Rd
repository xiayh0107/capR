% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agent-session.R
\name{cap_agent_step}
\alias{cap_agent_step}
\title{Advance an agent session by one model response}
\usage{
cap_agent_step(session, response, ..., prompt = NULL)
}
\arguments{
\item{session}{An active \code{capr_agent_session}.}

\item{response}{Model contract response: R list, JSON string, or JSON
file path (the same inputs as \code{\link[=cap_validate_response]{cap_validate_response()}}).}

\item{...}{Reserved.}

\item{prompt}{Optional exact prompt text handed to the model this turn;
defaults to the current digest text. Only its hash is recorded.}
}
\value{
The \code{capr.agent_turn.v1} record, invisibly appended to the
session:
one of outcome \code{answered}, \code{extended}, \code{denied_all},
\code{budget_exhausted}, \code{stale_source}, or \code{invalid_response}.
}
\description{
Validates the response, gates any follow-up requests, and applies the
approved patch. The session closes itself only on \code{stale_source}
(disclosure can never resume over a drifted source); every other outcome
leaves the session active so tool-driven hosts can let the model correct
itself. \code{\link[=cap_agent_run]{cap_agent_run()}} maps outcomes to stop reasons for the linear
loop.
}
