% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aisdk-tools.R
\name{cap_aisdk_tokenizer}
\alias{cap_aisdk_tokenizer}
\title{Budget tokenizer backed by aisdk token counting}
\usage{
cap_aisdk_tokenizer(model, id = NULL, version = "1.0.0")
}
\arguments{
\item{model}{An aisdk model: a \code{LanguageModelV1} object or a model id
string such as \code{"anthropic:claude-sonnet-5"} (strings are resolved by
aisdk at count time).}

\item{id}{Tokenizer id; defaults to the model's id, lowercased, invalid
characters replaced with \code{-}, and prefixed with \verb{aisdk-} (for example
\code{aisdk-claude-sonnet-5}).}

\item{version}{Semantic version recorded for the tokenizer.}
}
\value{
A \code{capr_tokenizer} with \code{provider = "aisdk"}.
}
\description{
Builds a \code{\link[=cap_tokenizer]{cap_tokenizer()}} whose \code{count} function calls
\code{aisdk::count_tokens()} on every rendered field, so digest budget
accounting matches what the model provider will actually charge.
Providers with a native counting endpoint (Anthropic
\verb{/messages/count_tokens}) yield model-exact counts; other providers use
aisdk's local heuristic estimate.
}
\details{
Unlike the built-in tokenizer, counting through this one MAY perform a
network call (the Anthropic endpoint), and aisdk falls back to its local
heuristic when that endpoint is unreachable: counts are model-exact when
online but are not guaranteed reproducible offline. capR itself still
ships zero network code -- the call happens inside aisdk, on the host's
behalf, only because the host installed this tokenizer. Each count runs
under the digest's per-field time limit, and any counting failure
surfaces as a typed \code{capr_tokenizer_invalid} error; budget accounting
never fails open.

Accounting is pinned: \code{\link[=cap_patch]{cap_patch()}} refuses any tokenizer other than the
one the digest was built with, so reuse the same tokenizer object (or
its registered id) for every follow-up patch of the same digest.
}
