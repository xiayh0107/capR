% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aisdk-tools.R
\name{cap_aisdk_ask}
\alias{cap_aisdk_ask}
\title{Schema-constrained ask function over aisdk structured output}
\usage{
cap_aisdk_ask(model, mode = c("tool", "json"), max_retries = 1L, ...)
}
\arguments{
\item{model}{An aisdk model: a \code{LanguageModelV1} object or a model id
string such as \code{"anthropic:claude-sonnet-5"}.}

\item{mode}{Structured-output mode: \code{"tool"} (default; the schema is one
forced tool call -- most reliable on models with native function
calling) or \code{"json"} (JSON parsed out of the model text).}

\item{max_retries}{How many times aisdk re-asks the model when its
output does not match the schema before handing the result to capR
anyway.}

\item{...}{Passed through to \code{aisdk::generate_object()} (for example
\code{system}, \code{temperature}, \code{max_tokens}).}
}
\value{
\verb{function(prompt)} returning a contract-response list, suitable
as the \code{ask} argument of \code{\link[=cap_agent_run]{cap_agent_run()}}.
}
\description{
Factory producing the \code{ask} callback for \code{\link[=cap_agent_run]{cap_agent_run()}}, implemented
with \code{aisdk::generate_object()}: the model is forced (\code{mode = "tool"}) or
instructed and re-asked (\code{mode = "json"}) to reply with an object that
parses as \code{cap.contract_response.v1}, which slashes \code{invalid_response}
turns and repair churn compared with free-text JSON replies.
}
\details{
capR never calls a model: this factory hands the network call to aisdk,
on the host's behalf, under the host's own provider credentials. Each
call returns aisdk's parsed object verbatim -- even when aisdk's own
schema check still failed after \code{max_retries} re-asks -- because
\code{\link[=cap_validate_response]{cap_validate_response()}} remains the semantic authority: a malformed
object becomes a typed \code{invalid_response} turn (repairable via
\code{cap_agent_run(max_repairs =)}), and a reply from which aisdk could
parse no object at all is \code{NULL}, which the core rejects fail-closed as
\code{capr_artifact_invalid}.
}
