# pack-table-basic Fixture

> Status: draft

This fixture validates that the `table-basic` Digest Pack can be discovered by
the reference implementation and exposes the expected field IDs and redactors.

It is intentionally narrower than `fixtures/basic-table/`: it tests pack loading
and metadata alignment, not digest rendering.
